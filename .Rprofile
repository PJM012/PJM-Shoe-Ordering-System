# Increase timeout for shinyapps.io
options(shiny.launch.browser = FALSE)
options(shiny.port = 8080)
options(shiny.host = "0.0.0.0")
options(shiny.autoreload = FALSE)

# Better SQLite settings
options(sqlite.log = FALSE)